export const checkNoEmptyID = (data) => {
    if (data.length === 0) {
        return false
    }
    return true
 }
export const checkNoEmptyNombre = (data) => {
    if (data.length === 0) {
        return false
    }
    return true
 }
export const checkNoEmptyApellido = (data) => {
    if (data.length === 0) {
        return false
    }
    return true
 }
export const checkNoEmptyDirección = (data) => {
    if (data.length === 0) {
        return false
    }
    return true
 }

